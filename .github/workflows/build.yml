name: Build

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      # ここで直接ボードとシールドを指定します
      board: nice_nano_v2
      shield: crosses
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
